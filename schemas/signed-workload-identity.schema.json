{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://regagentops.dev/schemas/signed-workload-identity.schema.json",
  "title": "RegAgentOps Signed Workload Identity",
  "type": "object",
  "additionalProperties": false,
  "required": ["statement", "key_id", "algorithm", "signature_base64url", "signing_document_digest", "schema_version"],
  "properties": {
    "statement": {
      "type": "object",
      "additionalProperties": false,
      "required": ["institution_id", "agent_id", "human_owner_id", "model_provider", "model_id", "workload_id", "challenge_digest", "issued_at", "expires_at", "schema_version"],
      "properties": {
        "institution_id": {"type": "string", "minLength": 1, "maxLength": 256},
        "agent_id": {"type": "string", "minLength": 1, "maxLength": 256},
        "human_owner_id": {"type": "string", "minLength": 1, "maxLength": 256},
        "model_provider": {"type": "string", "minLength": 1, "maxLength": 256},
        "model_id": {"type": "string", "minLength": 1, "maxLength": 256},
        "workload_id": {"type": "string", "minLength": 1, "maxLength": 256},
        "challenge_digest": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
        "issued_at": {"type": "string", "format": "date-time"},
        "expires_at": {"type": "string", "format": "date-time"},
        "schema_version": {"const": "regagentops.workload-identity-statement.v1"}
      }
    },
    "key_id": {"type": "string", "minLength": 1, "maxLength": 256},
    "algorithm": {"const": "Ed25519"},
    "signature_base64url": {"type": "string", "pattern": "^[A-Za-z0-9_-]{86}$"},
    "signing_document_digest": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "schema_version": {"const": "regagentops.signed-workload-identity.v1"}
  }
}
