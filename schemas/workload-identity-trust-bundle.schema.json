{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://regagentops.dev/schemas/workload-identity-trust-bundle.schema.json",
  "title": "RegAgentOps Workload Identity Trust Bundle",
  "type": "object",
  "additionalProperties": false,
  "required": ["institution_id", "keys", "schema_version"],
  "properties": {
    "institution_id": {"type": "string", "minLength": 1, "maxLength": 256},
    "keys": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["institution_id", "key_id", "public_key_base64url", "not_before", "not_after", "status", "algorithm"],
        "properties": {
          "institution_id": {"type": "string", "minLength": 1, "maxLength": 256},
          "key_id": {"type": "string", "minLength": 1, "maxLength": 256},
          "public_key_base64url": {"type": "string", "pattern": "^[A-Za-z0-9_-]{43}$"},
          "not_before": {"type": "string", "format": "date-time"},
          "not_after": {"type": "string", "format": "date-time"},
          "status": {"enum": ["active", "retired", "disabled"]},
          "algorithm": {"const": "Ed25519"}
        }
      }
    },
    "schema_version": {"const": "regagentops.workload-identity-trust-bundle.v1"}
  }
}
